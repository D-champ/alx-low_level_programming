#include "main.h"
/**
 * _abs - writing a function that computes the absolute value of an integer
 * @n: integer input
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
